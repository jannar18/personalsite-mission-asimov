# Personal Site — Mission Asimov

**All project context lives in `CLAUDE.md`.** Read that file — it is the single source of truth for project stage, repository structure, key decisions, design language, conventions, debugging heuristics, lessons learned, and Lattice task tracking.

This file exists so that non-Claude agents (Codex, Gemini, etc.) have an entry point. The content is not duplicated here to prevent drift.
